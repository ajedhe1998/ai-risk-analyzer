def load_jenkins_context():
    # Simulated MCP connector for Jenkins build data
    return [
        "Last pipeline had 2 flaky UI test failures.",
        "Deployment rollback occurred in previous release due to config mismatch.",
    ]
