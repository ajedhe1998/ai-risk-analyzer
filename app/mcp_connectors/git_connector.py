def load_git_context():
    # Simulated MCP connector for Git data
    return [
        "High code churn detected in payment module during last 3 days.",
        "Multiple hotfix commits merged close to release cut-off.",
    ]
